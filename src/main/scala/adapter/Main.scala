package adapter

/**
  * @author Evgeny Borisov
  */
class Main {
  def main(args: Array[String]): Unit = {
   val xxxx = List(ProductRepo.getProduct())
//    Calc.totalPrice(xxxx) todo make it compile with only import

  }
}
